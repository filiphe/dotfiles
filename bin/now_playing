#!/bin/sh

error="error: Connection refused"
if [[ -z $(mpc) ]]; then
	echo "p  mopidy not running"
elif [[ $(mpc | tail -n 2 | head -n 1 | awk '{print $1}' | tr -d '[]') == "paused" ]]; then
	echo "p  mopidy is paused"
else
	echo "p  " $(mpc current)
fi
