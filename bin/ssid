#!/bin/sh

ssid=$(iwgetid -r)
signal=$(cat /proc/net/wireless | tail -n 1 | awk '{print $3}' | tr -d '.')
max=70
if [[ $signal -lt 35 ]]; then
	echo 'w '$ssid
fi
if [[ $signal -gt 35 ]]; then
	echo 'w '$ssid
fi
